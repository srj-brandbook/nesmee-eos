# Public app URL (HTTPS via Dokploy Traefik). Used for CORS, cookies, and email links.
APP_URL=https://eos.aibrandroid.com

# MongoDB (internal only — do not expose ports)
MONGO_ROOT_USER=nesmee
MONGO_ROOT_PASSWORD=49bb2e80c09f41da93e6492b400c48c05fe61d3d411faba450c6e5dfabfb8363
MONGO_DB=nesmee_eos

# Auth cookies
COOKIE_SECRET=6fa209e70a5a1cb316b01bcaea032e9a7dbdbedc018b2d81e7130ebfb6e42adb
COOKIE_NAME=sid
REFRESH_COOKIE_NAME=rid
COOKIE_SAMESITE=lax

# Optional auth timings
ACCESS_TTL_MINUTES=15
SESSION_TTL_HOURS=168
IDLE_TTL_HOURS=12
PASSWORD_RESET_TTL_MINUTES=60
EMAIL_VERIFY_TTL_HOURS=24
LOGIN_MAX_ATTEMPTS=5
LOGIN_LOCK_MINUTES=15

# SMTP (real provider — Mailhog is local-only)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=Nesmee EOS <noreply@example.com>

# First-time seed (run once: docker compose -f docker-compose.prod.yml exec nesmee-api npm run seed)
SUPERADMIN_EMAIL=admin@example.com
SUPERADMIN_PASSWORD=ChangeMeNow!23
SUPERADMIN_NAME=Super Admin

# Cloudinary
CLOUDINARY_CLOUD_NAME=jamgx6yu
CLOUDINARY_API_KEY=234639753436915
CLOUDINARY_API_SECRET=XMdbi-pn7GeP29qfEMS5K_De_PQ
CLOUDINARY_FOLDER=nesmee/eos

# Frontend branding (build-time)
NEXT_PUBLIC_APP_NAME=Nesmee EOS
NEXT_PUBLIC_APP_TAGLINE=Export operating system
NEXT_PUBLIC_APP_SHORT_TAGLINE=Alpha v.0.0.3

# Leave empty so the browser calls same-origin /api (Next rewrites to backend)
# NEXT_PUBLIC_API_URL=

LOG_LEVEL=info
