# Frontend

Next.js App Router UI for Nesmee EOS, the export operating system.

```bash
copy .env.example .env.local
npm install
npm run dev
```

- Port: `3000`
- Leave `NEXT_PUBLIC_API_URL` empty in development to use the `/api` rewrite.

See `../docs/frontend.md`.
