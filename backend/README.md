# Backend

Express API for the SaaS boilerplate.

```bash
copy .env.example .env
npm install
npm run seed
npm run dev
```

- Port: `5000`
- API prefix: `/api/v1`
- Health: `GET /health`

See `../docs/backend.md` and `../docs/api.md`.
